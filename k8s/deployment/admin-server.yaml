apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin-server
  namespace: petclinic
  labels:
    app: petclinic
spec:
  replicas: 1
  selector:
    matchLabels:
      app: petclinic
      tier: admin-server
  template:
    metadata:
      labels:
        app: petclinic
        tier: admin-server
    spec:
      containers:
        - name: app
          image: 22127063/spring-petclinic-admin-server:latest
          imagePullPolicy: Always
          resources:
            requests:
              memory: 512Mi
            limits:
              memory: 512Mi
          envFrom:
            - configMapRef:
                name: app
            - secretRef:
                name: database
          ports:
            - name: http
              containerPort: 9090
---
apiVersion: v1
kind: Service
metadata:
  name: admin-server
  namespace: petclinic
spec:
  type: NodePort
  selector:
     app: petclinic
     tier: admin-server
  ports:
    - port: 9090
      targetPort: 8080
      nodePort: 30090